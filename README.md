# matchup-predictor
ML pipeline + local web app for UFC and basketball outcome prediction. Sweeps 4,032 model/scaler/sampling/outlier combinations, runs PyTorch and pretrained tabular foundation models, and explains every live prediction in plain language.
